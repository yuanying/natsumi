# The only commands in the natsumi-tools image (ADR 0011, ADR 0018), one per line, installed as /bin/<name>.
# Reading memory, and writing it: nothing that reaches a network, installs packages, runs an interpreter or runs git.
# The run_memory_shell tool describes this list (src/server/memory-shell.ts); a test keeps the two equal.
sh
cat
find
grep
head
ls
rg
sort
tail
uniq
wc
mkdir
mv
cp
rm
sed
awk
